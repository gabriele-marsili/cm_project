# Changes pre-consegna codice — sessione 2026-06-09

Revisione critica globale prima della creazione dello zip del codice. Il report
era già approvato dal prof (email 2): obiettivo della sessione = confermare la
chiusura dei 4 rilievi, escludere numeri allucinati/incoerenti, sistemare lo
stile (ripetizioni), e preparare il deliverable codice riproducibile.

- Commit di riferimento: `545328e` ("zip file, code improvements, review").
- Stato consegna: report ricompila a 69 pagine, 53/53 test passano, 0 reference
  irrisolte, 0 citazioni `.csv` nel report. Zip 1.19 MB, 89 file.

> Notazione usata in questo file: `γ`, `δ`, `β`, `α`, `ε`, `λ`, `ρ`, `√`, `²`,
> `≤`, `≥`, `·`, `‖·‖`, `f*`, `·10^n`. Niente LaTeX inline.

---

## 1 · Causa radice trasversale: gap ASSOLUTI presentati come RELATIVI

Dopo la correzione del prof (usare gap relativi ovunque), il report dichiara in
§5.2 che ogni gap del capitolo è relativo `(f − f*)/|f*|`. Ma **5 CSV salvano il
gap ASSOLUTO** `f − f*`:

- `convergence_instance.csv`, `rho_sweep.csv`, `gamma_floor_test.csv`,
  `delta0_families.csv`, `sgptl_long_run.csv`.

I CSV che invece salvano già il relativo: `warm_cold_irls_*`, `warm_cold_sgptl_*`,
`comparison_table.csv`, `real_data_crossing.csv`, `real_data_irls_crossing.csv`.

Alcuni numeri del report presi **direttamente** dai CSV assoluti erano quindi
assoluti spacciati per relativi. Tutti riprodotti da seed 42 per ricavare il
relativo vero. Riferimenti riprodotti: `f*` istanza convergenza (H=100, M=300) =
1.1322; `f*` istanza params (H=100, M=400) = 1.156469.

I ratio (Tab. 5.4 admissible/oracle) e i conteggi di iterazione/contrazione sono
invarianti per la divisione per `f*` e **non** sono stati toccati.

---

## 2 · Correzioni numeriche (report)

| File / sezione | prima | dopo | nota |
|---|---|---|---|
| `5_results/results.tex` §5.2 | `f(0) ≈ 76` | `f(0) ≈ 4.6` | valore stale/sbagliato; `f(0) = ½‖y‖² = 4.57`. Non presente in alcun CSV → mai catturato dal check "realign vs CSV". Già errato nella consegna 2 approvata (riga 1134 del PDF). |
| §5.2 | crossing `(f−f*)/|f*| < 10⁻⁶` a iter `122` | iter `115` | `122` era il crossing del gap ASSOLUTO. |
| §5.2 | gap rel `@1500` = `1.5·10⁻¹⁰` | `1.3·10⁻¹⁰` | `1.5·10⁻¹⁰` era l'assoluto (= `1.34·10⁻¹⁰ · f*`). |
| §5.4 | ρ-sweep `… 6.1·10⁻⁴` | `1.7·10⁻⁵ (ρ=0.3) … 7.0·10⁻⁴ (ρ=0.95)` | estremo basso `1.7·10⁻⁵` confermato come relativo corretto; estremo alto ricavato (`8.115·10⁻⁴ / 1.156 = 7.02·10⁻⁴`). |
| §5.4 | best ρ per-istanza `0.3, 0.3, 0.7` | `0.8, 0.3, 0.7` | `smoke_rho.csv`: minimo gap sintetico a ρ=0.8 (`3.35·10⁻⁸`), non 0.3 (`5.4·10⁻⁷`, 16× peggio). diabetes (0.3) e california (0.7) erano già giusti. |
| §5.4 | δ0 c-sweep `[4.2·10⁻⁵, 1.2·10⁻⁴]` | `[4.8·10⁻⁵, 1.4·10⁻⁴]` | range riprodotto a rho=0.7, i_max=5000. Il `4.2·10⁻⁵` era cross-pasted dall'istanza di convergenza (M=300). |
| §5.4 tabella γ-floor | `4.4·10⁻²` / `1.1·10⁻⁴` | `3.7·10⁻²` / `1.0·10⁻⁴` | media RELATIVA sui 5 seed (H=100, M=300); i valori vecchi erano la media ASSOLUTA del CSV. |
| §5.4 prosa γ-floor | "stuck at ≈ `4.4·10⁻²`" | "≈ `3.7·10⁻²`" | coerenza con la tabella. |
| `appendix/appendix.tex` App.B | `940` (warm) / `922` (cold) | `941` / `923` | allineamento con Tab. 5.1 e `warm_cold_irls_synthetic.csv`. |

**Affermazione falsa rimossa** (§5.4): "ρ∈[0.3,0.7] entro un fattore 2 dal best
ovunque" — falsa sul sintetico, dove ρ=0.3 è 16× peggio del best (ρ=0.8).
Sostituita con la storia corretta: ranking instance-dependent, sui due dataset
reali il gap finale è piatto in ρ (spread < 1%), sul sintetico il mid-range è
nettamente preferito (ρ=0.95 è ~4 ordini peggio di ρ=0.8). Default ρ=0.7 (entro
1.4× del best sintetico, best-or-flat sui reali).

**Verificati corretti e NON cambiati**: `0.37` (gap0 relativo: `0.4232/1.1322 =
0.374`); MSE SGPTL diabetes `0.898` (riprodotto: crossing a iter 60482, gap rel
`9.18·10⁻⁷`, `w_best` → MSE `0.8983`); contrazioni `7 → 91`; tutte le tabelle
principali (5.1, 5.3, 5.4-ratio, 5.5, 5.6, 5.7, 5.8, 5.9, solver).

---

## 3 · Rimozione di tutte le citazioni `.csv` dal report

Su richiesta esplicita: nessun nome di file `.csv` deve comparire nel report.
Rimosse 7 occorrenze (2 introdotte e poi tolte in §5.4 + 5 pre-esistenti):

- caption Tab. 5.1 — `warm_cold_irls_{synthetic,real}.csv`,
  `warm_cold_sgptl_{synthetic,real}.csv`.
- §5.3 "Defaults" — `warm_start_cost.csv`.
- §5.7 — `real_data_crossing.csv`.
- caption Tab. 5.6 — `convergence_instance.csv` (sostituito con "output of the
  current implementation").

Verifica: `grep -rl '\.csv'` sulle sezioni del report → ZERO.

---

## 4 · Ripetizioni cross-sezione cancellate (de-LLM, rilievo esplicito del prof)

Risolte per **cancellazione** delle occorrenze non-canoniche (una sede + cross-ref),
non per parafrasi. Sei concetti ripetuti 3–5 volte:

- "‖w0 − w*‖ entra moltiplicativamente" — rimossa la ripetizione in `results.tex:284`.
- "OLS già vicino a f* su california" — collassata la doppia spiegazione in appendice
  (rimossa dalla caption della tabella δ0, tenuta nel corpo).
- "smoothed minimiser differisce da LASSO di O(ε)" — `algo1.tex:224` (duplicato
  quasi-adiacente di item 2 a riga 230) ridotto a rimando.
- "rate ≈ 1 perché la penale è affine" — `results.tex:131` ridotto a cross-ref ad
  Appendice A (sede canonica della derivazione).
- "il bound limita il record, non l'iterato" — rimossa la parentetica in `results.tex:150`.
- "per-decade costante IRLS / ~100× SGPTL" — `results.tex:831` ridotto a cross-ref.

Inoltre: rimosso "the signature of" (frasetta promozionale); precisato il claim
sui test → "surrogate-descent (IRLS) and record-monotonicity (SGPTL) invariants".

---

## 5 · Deliverable codice (per lo zip)

- **`progetto/code/requirements.txt`** (nuovo): numpy/scipy/scikit-learn/matplotlib;
  cvxpy e pytest segnati opzionali (cvxpy serve solo al cross-check Clarabel, i cui
  risultati sono già nei CSV).
- **`.gitignore`**: aggiunto `.pytest_cache/`.
- **`progetto/CM_project25_group63_code.zip`** (nuovo): 89 file, 1.19 MB. Costruito
  da copia staged escludendo venv (`lib/` 208 MB, `bin/`, `share/`, `pyvenv.cfg`),
  `__pycache__`, `.pytest_cache`, `results_old_submission/`, `experiments/old/`,
  `utils/` (note di sviluppo interne), `.DS_Store`. Import di `src` verificato dalla
  copia; IRLS gira (gap `2.5·10⁻⁸`).

---

## 6 · Verifiche eseguite

- `pytest tests/`: **53/53 pass** in ~2.2 s.
- `latexmk -pdf main.tex`: 69 pagine, 0 reference/citation irrisolte, 0 `.csv`.
  `main.pdf` più recente di tutte le `.tex` (nessuna modifica pendente).
- 10 figure renderizzate e ispezionate: leggibili, scala log, label coerenti.
  In particolare `convergence_vs_iter` (la "Figura 2" dei rilievi prof): le due
  metà ora plottano la **stessa** quantità `(f−f*)/|f*|`; pannello sinistro
  semilog con coda retta + fit `≈0.995` → rilievi prof su quella figura chiusi.
- Riproduzioni da seed 42 per ogni numero modificato (vedi §1–§2).

### Rilievi email prof 2 — stato di chiusura
- Boundedness via sublevel set Sc0 → ora argomento di contrazione della distanza
  a w* (Fejér); nessun residuo sugli iterati DSM. **Chiuso.**
- Figura 2, due nomi diversi nelle due metà → stessa quantità in entrambe. **Chiuso.**
- Gap relativi (anche Tab. 5.1) → tutto relativo; rimossi i residui assoluti
  spacciati per relativi (§1–§2). **Chiuso.**
- Curva sinistra "non lineare" → semilog con coda retta + fit + dimostrazione in
  Appendice A (Ostrowski, ρ = 1 − μ_min < 1). **Chiuso.**

---

## 7 · Punti aperti (non bloccanti)

**Figura `delta0_families`** (15 pannelli, 5×3, in appendice): leggibile ma al
   limite del §4.6 ("molti mini-plot"). Cosmetica; valutare una riga rappresentativa
   nel corpo + descrizione del resto.
