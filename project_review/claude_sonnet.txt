Review post-modifiche — Issue residue
Problema 1 (CRITICO) — 6_conclusions/conclusions.tex righe 107–108: affermazione fuorviante sui dati reali
Testo attuale:

"finds that IRLS matches or slightly improves on the sklearn reference on both"

Perché è sbagliato. Il §5.7 aggiornato ora spiega correttamente che IRLS raggiunge un training objective più basso di sklearn su entrambi i dataset, ma su diabetes il test-MSE è peggiore (0.898 vs 0.745 sklearn), perché sklearn si ferma prima per effetto di early stopping che generalizza meglio. Dire che "IRLS matches or slightly improves on the sklearn reference" senza qualificare rispetto a quale metrica è fuorviante e contraddice §5.7.
Fix proposto (sostituire righe 106–109):
latexhousing}) by using sklearn's high-tolerance coordinate descent as the
$f^{*}$ reference, and finds that IRLS achieves a lower training
objective than sklearn's budget-limited coordinate descent on both
datasets; the test-MSE comparison is dataset-dependent (IRLS matches
sklearn on California but the sklearn early-stop generalises better
on diabetes), consistent with the gap in training accuracy not
translating directly to held-out performance. SGPTL converges on the larger
$M\gg H$ instance and stalls on the smaller $M\sim H$ one --- exactly
the rate-driven regime split the synthetic experiments predict.

Problema 2 (MINORE) — 5_results/results.tex riga 699: caption scalability troppo assertiva rispetto al corpo testo
Caption (riga 699):

"IRLS' total cost departs from the cubic prediction at H≥1000H\ge 1000
H≥1000, where the Cholesky factor outgrows the L2 cache."

Corpo testo (righe 675–683): hedgia esplicitamente — "The most likely explanation is a memory-hierarchy effect" e "We do not claim this as a definitive cause without explicit cache profiling."
La caption afferma la tesi come fatto, il corpo la presenta come ipotesi. Le didascalie devono essere consistenti col corpo.
Fix proposto per riga 699:
latex    $H\ge 1000$, where a memory-hierarchy effect most likely penalises the inner Cholesky solves.}

Problema 3 (INFORMATIVO) — \eqref{eq:proof_perstep} — label confermata esistente
Il label eq:proof_perstep esiste a riga 310 di chapter3.tex — nessuna azione necessaria.

Problema 4 (DA VERIFICARE) — experiment_params.py: k_max nel codice vs report
Il report (§5.6, solver comparison) ora cita k_max=300 e lo script experiment_comparison.py usa k_max=300. Ma experiment_params.py ha k_irls_solvers = 300 nel codice (riga 179) mentre il suptitle alla riga 231 dice k_max={k_irls_solvers} — quindi ora è consistente se la variabile vale 300. Verificare che le figure salvate in results/figures/solver_comparison.pdf corrispondano a questo run (non a un run precedente con k_max=2000). Se le figure non sono state rigenerate dopo la modifica, i valori nella tabella del report (12.5ms, 265 iter) potrebbero non matchare il PDF.
Azione: rieseguire python experiments/experiment_params.py per rigenerare solver_comparison.pdf con i parametri correnti, oppure confermare che il run attuale ha già prodotto quei numeri.

I due fix urgenti sono il Problema 1 (conclusioni fuorvianti sui dati reali) e il Problema 2 (caption scalability). Il Problema 3 è risolto, il Problema 4 richiede solo una verifica di coerenza figure/script.