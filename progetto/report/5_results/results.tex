\chapter{Experimental Results}
\label{chap:results}

This chapter tests the IRLS and SGPTL implementations against the
predictions of Chapters~\ref{chap:irls}--\ref{chap:dsm} (convergence
behaviour on ELM~LASSO) and against the cost--accuracy comparison of
Chapter~\ref{chap:comparison}.

% =====================================================================
\section{Experimental setup and datasets}
\label{sec:setup}

\paragraph{Implementation.}
The code under \texttt{progetto/code/src/} is split into five modules:
\texttt{lasso\_utils.py} (objective, smooth-part gradient, subgradient
selection), \texttt{linear\_}\allowbreak\texttt{solvers.py} (Cholesky via
\texttt{scipy.linalg.cho\_factor/}\allowbreak\texttt{cho\_solve} and a
hand-rolled Jacobi-preconditioned CG), \texttt{irls.py}
(Algorithm~\ref{alg:irls}),
\texttt{deflected\_}\allowbreak\texttt{subgradient.py}
(Algorithm~\ref{alg:dsm}), and \texttt{elm.py} (random hidden layer).
Core algorithms are written from scratch (reusing only NumPy/SciPy as
primitives). A pytest suite cross-checks both algorithms against
\texttt{sklearn.linear\_}\allowbreak\texttt{model.Lasso} and verifies the
surrogate-descent and record-monotonicity invariants iterate by iterate.

\paragraph{Reference solution.}
We obtain $\mathbf{w}^{*}$ from
\texttt{sklearn.linear\_}\allowbreak\texttt{model.Lasso} (coordinate
descent, \texttt{tol}$=10^{-12}$, $10^{5}$ iterations,
\texttt{fit\_intercept=False}). Sklearn's loss has a $1/(2M)$ prefactor on the squared residual and
weights the $\ell_{1}$ term by $\alpha$, so the equivalent
of~\eqref{eq:original_problem} is obtained by passing
$\alpha_\text{sklearn} = \lambda_\text{LASSO}/M$; we then evaluate
$f^{*} = f(\mathbf{w}^{*})$ from~\eqref{eq:original_problem} directly. The returned
$\mathbf{w}^{*}$ satisfies the optimality
condition~\eqref{eq:optimality} below $10^{-3}$ on every instance. On the
ELM-transformed real data, where coordinate descent does not converge in
a realistic budget, we replace this reference with the IRLS-converged
value cross-validated against CVXPY's interior-point solver Clarabel
(Section~\ref{sec:real_data}).

\subsection{Datasets}
\label{sec:datasets}
We use three instances spanning two conditioning regimes.

\emph{Synthetic LASSO benchmark} (\texttt{src/data\_generation.py}). We
sample a sparse $\mathbf{w}_\text{true}\in\mathbb{R}^{H}$
($10$--$15\%$ non-zero, entries $\sim\mathcal{N}(0,1)$), draw
$\mathbf{X}\in\mathbb{R}^{M\times H}$ with i.i.d.\ Gaussian columns
normalised to unit $\ell_{2}$ norm, and set
$\mathbf{y} = \mathbf{X}\mathbf{w}_\text{true} + \boldsymbol{\varepsilon}$,
$\boldsymbol{\varepsilon}\sim\mathcal{N}(0, 0.05^{2}\mathbf{I})$. This is the benign regime for LASSO. We pick $M\ge 2H$ to keep
$\kappa(\mathbf{X}^{\top}\mathbf{X})$ at a moderate constant by
random-matrix concentration~\cite{hastie2009elements}, the planted
$\mathbf{w}_\text{true}$ provides the ground truth for support-recovery
metrics, and the noise std $0.05$ puts the SNR in the moderate-noise
regime (about $5$--$10\%$ of the signal magnitude). It validates the
algorithms in a well-conditioned setting but does \emph{not} reproduce the
conditioning of ELM data.

\emph{ELM-transformed real datasets} (\texttt{diabetes},
\texttt{california}). We load \texttt{diabetes} ($n=442$, $d=10$)
\cite{efron2004least} and \texttt{california\_housing} ($n=20640$,
$d=8$) \cite{pace1997sparse} from scikit-learn, split $80/20$,
standardise features and target on the train split, and apply the ELM
map $\mathbf{X}_\text{hidden}=\sigma(\mathbf{X}\,\mathbf{W}_{1}^{\top})$
with $H$ sigmoid units and a fixed random $\mathbf{W}_{1}$. We use
$H=200$ for the main experiments and $H=50$ where an independent
solver (\texttt{cvxpy.CLARABEL}) is affordable for cross-validation. The
sigmoid breaks the unit-norm column property and inflates
$\kappa(\mathbf{X}_\text{hidden}^{\top}\mathbf{X}_\text{hidden})$ to
$\sim 10^{6}$ on both datasets, orders of magnitude above the synthetic
design. This is the ill-conditioned regime targeted by the experiments.

\emph{Limitations.} (i)~The synthetic benchmark lives in the
well-conditioned, incoherent regime by construction, so positive results
there do not extrapolate to ill-conditioned designs. (ii)~The real
datasets have no ground-truth support, so the
\emph{support-recovery} metrics of Section~\ref{sec:quality}
(precision/recall/F$_1$ against the planted support) apply only to the
synthetic instance; sparsity counts at a fixed threshold remain
well-defined and are reported on real data alongside training-objective
gaps and test MSE. (iii)~On
\texttt{california} at $\lambda_\text{LASSO}=0.1$ the OLS warm start
already sits very close to $f^{*}$ (Section~\ref{sec:warm_cold}); we
account for this in the warm-vs-cold and real-data comparisons.

% =====================================================================
\section{Convergence and agreement with theory}
\label{sec:convergence}

\paragraph{Setting.}
Synthetic convergence instance, $H=100$, $M=300$,
$\lambda_\text{LASSO}=0.1$, sparsity $10\%$. Both algorithms are run from
the same OLS warm start
$\mathbf{w}_{0}=(\mathbf{X}^{\top}\mathbf{X})^{-1}\mathbf{X}^{\top}\mathbf{y}$
so that any difference in the curves is attributable to the algorithm and
not to the initialisation; the one-time Cholesky cost of forming
$\mathbf{w}_{0}$ (Section~\ref{sec:dsm_parameters}) is charged to both
wall-clock totals. The relevant objective values of this instance are
$f(\mathbf{0})\approx 76$ at the origin, $f(\mathbf{w}_{0})\approx 1.56$
at the warm start, and $f^{*}\approx 1.13$ at the sklearn reference,
which puts the initial relative gap
$(f(\mathbf{w}_{0})-f^{*})/|f^{*}|\approx 0.37$ (the $y$-intercept of
Figure~\ref{fig:convergence-iter}). Every gap figure and table in this
chapter reports the \emph{relative} gap $(f-f^{*})/|f^{*}|$: an absolute
gap is not comparable across instances, since $f^{*}$ ranges from
$\approx 1.1$ on the synthetic to $\approx 2.4\cdot 10^{3}$ on
\texttt{california}, so a fixed absolute gap is loose at large $f^{*}$ and
tight at small $f^{*}$.

\paragraph{IRLS: linear rate (Section~\ref{sec:irls_convergence}).}
Chapter~\ref{chap:irls} predicts local linear convergence of the MM
iteration to the smoothed minimiser, with an asymptotic factor close to
$1$. Figure~\ref{fig:convergence-iter} (left) shows the two regimes that
\emph{local} entails. Up to iteration $\approx 250$ the curve is not
straight, for two reasons: the active set is first identified (as
components cross $\varepsilon_\text{thr}$ the reweighting map changes from
step to step, so no single linear map is iterated yet), and once the
support has frozen the faster modes of the resulting fixed contraction
decay before the slowest mode, which sets the asymptotic rate, dominates.
Throughout this pre-asymptotic phase the gap falls \emph{faster} than the
asymptotic rate (below $10^{-6}$ already by iteration $122$). The curve
then straightens on the semilog axis: the per-iteration factor settles to
$\approx 0.995$ (the dashed fit over iterations $400$--$1200$), the
signature of linear convergence, and the relative gap reaches
$1.5\cdot 10^{-10}$ at $1500$ iterations. IRLS is thus globally convergent
(Section~\ref{sec:irls_convergence}) yet only \emph{locally} linear: a
constant rate from $k=0$ is neither observed nor claimed, since the rate
theorem governs the tail, not the pre-asymptotic transient. That the factor is so close to $1$ is the loose quadratic
majorisation of $|w|$ on the active components
(Section~\ref{sec:irls_convergence}); it is nearly independent of
$\varepsilon_\text{thr}$. We set $\varepsilon_\text{thr}=10^{-12}$ on this
figure so the floor does not mask the linear tail; at the default
$\varepsilon_\text{thr}=10^{-8}$ (Section~\ref{sec:params}) the gap
flattens at the $O(\varepsilon_\text{thr})$ smoothing floor near iteration
$800$.

\paragraph{SGPTL: sublinear rate (Theorem~\ref{thm:dsm_convergence}).}
The expected behaviour from Chapter~\ref{chap:dsm} is an $O(1/\sqrt{i})$
record gap and a non-monotone current value. Figure~\ref{fig:convergence-iter} (right,
$8000$ iterations, $\beta=1$, $\gamma_{\min}=0.05$,
$\delta_{0}=0.1\,f(\mathbf{w}_{0})$, $\rho=0.7$) confirms both: the
record follows the sublinear envelope down to a relative gap
$4.2\cdot 10^{-5}$, and each decade of accuracy costs a $\sim\!100\times$
increase in iterations, exactly the $\varepsilon\mapsto\varepsilon/10
\Rightarrow i\mapsto 100\,i$ scaling of the $O(L^{2}/\varepsilon^{2})$
bound. The current value oscillates around the record, as the theory
allows (the bound controls the record, not the raw iterate).

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/convergence_vs_iter.pdf}
    \caption{Relative gap $(f-f^{*})/|f^{*}|$ versus iteration on $H=100$,
    $M=300$, $\lambda_\text{LASSO}=0.1$. Both panels plot the same
    quantity; the axis type differs because each linearises its own rate.
    Left: IRLS on a semilog axis, where local linear convergence appears
    as a straight line; the early support-identification transient is
    steeper, and the tail matches the dashed fit (per-iteration factor
    $\approx 0.995$), shown with $\varepsilon_\text{thr}=10^{-12}$ so the
    smoothing floor does not mask it. Right: SGPTL on log--log axes; the
    record (red, Theorem~\ref{thm:dsm_convergence}) stays at or below the
    $O(1/\sqrt{i})$ envelope (grey dashed, $g_{0}^{\mathrm{rel}}/\sqrt{i}$),
    consistent with the sublinear bound, while the current value (orange,
    subsampled log-uniformly) oscillates above it.}
    \label{fig:convergence-iter}
\end{figure}

Against wall-clock time (Figure~\ref{fig:convergence-time}) SGPTL's
cheaper iteration ($O(MH)$ vs $O(H^{3})$) does not offset the slower
rate at this size: IRLS reaches relative gap $10^{-6}$ in $\sim\!5$~ms
and $10^{-10}$ by $\sim\!60$~ms, while SGPTL drops below $10^{-2}$ within
$\sim\!4$~ms and then crawls to $\sim\!4\cdot 10^{-5}$ over
$\sim\!150$~ms.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.85\textwidth]{images/convergence_vs_time.pdf}
    \caption{Relative gap $(f-f^{*})/|f^{*}|$ versus CPU time on the
    instance of Figure~\ref{fig:convergence-iter}, log--log axes.}
    \label{fig:convergence-time}
\end{figure}

Both convergence curves above track the SGPTL \emph{record}
$\bar f^{\,i} = \min_{h\le i} f(\mathbf{w}_{h})$ rather than the raw
current value $f(\mathbf{w}_{i})$: as anticipated in
Section~\ref{sec:dsm_target_level}, the subgradient dynamics are not
monotone, and only the record carries the rate guarantee of
Theorem~\ref{thm:dsm_convergence}. Figure~\ref{fig:dsm-nonmonotone} makes
this explicit: the current gap spikes well above the record at several
points along the run, while the record is monotone by construction. This
is also the practical reason the algorithm tracks and returns
$\mathbf{w}_\text{best}$ rather than the last iterate.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/dsm_nonmonotone.pdf}
    \caption{SGPTL on the convergence instance, first $2000$ iterations,
    semilog $y$, relative gap $(f-f^{*})/|f^{*}|$. The current relative gap
    (orange) overshoots the record (black) repeatedly; the largest
    overshoot is annotated.}
    \label{fig:dsm-nonmonotone}
\end{figure}

% =====================================================================
\section{Cold versus warm start}
\label{sec:warm_cold}

\paragraph{Setting.}
We run \emph{both} algorithms from \emph{both}
initialisations on all three instances: the synthetic
benchmark ($H=100$, $M=300$, $\lambda_\text{LASSO}=0.1$) and the two
ELM-transformed real datasets ($H=200$). Both Theorem~\ref{thm:irls_convergence}
(IRLS) and Theorem~\ref{thm:dsm_convergence} (SGPTL) admit any
$\mathbf{w}_{0}$; what changes between the starts is the initial distance
$\norm{\mathbf{w}_{0}-\mathbf{w}^{*}}$, which enters their convergence
bounds multiplicatively. Table~\ref{tab:warm-cold-summary} collects the
final gaps; the rest of the section interprets them.

\begin{table}[H]
    \centering
    \small
    \setlength{\tabcolsep}{8pt}
    \small
    \setlength{\tabcolsep}{6pt}
    \begin{tabular}{llrrr}
        \toprule
        algorithm & start & synthetic & \texttt{diabetes} & \texttt{california} \\
        \midrule
        IRLS  & warm & $1.6\cdot 10^{-8}$  ($941$)  & $1.1\cdot 10^{-10}$ ($894$)   & $6.1\cdot 10^{-13}$ ($102$) \\
        IRLS  & cold & $1.6\cdot 10^{-8}$  ($923$)  & $6.1\cdot 10^{-11}$ ($1\,118$) & $6.1\cdot 10^{-13}$ ($115$) \\
        SGPTL & warm & $4.2\cdot 10^{-5}$           & $1.5\cdot 10^{-2}$      & $2.0\cdot 10^{-4}$ \\
        SGPTL & cold & $2.1\cdot 10^{-5}$           & $4.9\cdot 10^{-3}$      & $1.8\cdot 10^{-2}$ \\
        \bottomrule
    \end{tabular}
    \caption{Final relative gap $|f(\mathbf{w}_\text{final})-f^{*}|/|f^{*}|$
    on each instance; the parenthesised number after each IRLS entry is the
    iteration count at which $\varepsilon_\text{stop}=10^{-8}$ triggers. On
    the real data both starts reach the $O(\varepsilon_\text{thr}=10^{-8})$
    smoothing floor, so the warm vs cold contrast is in iteration count, not
    final accuracy; on the synthetic the floor is the
    $\varepsilon_\text{thr}=10^{-8}$ value against the sklearn $f^{*}$. SGPTL
    runs $8\,000$ iterations everywhere with the defaults of
    Section~\ref{sec:params}; here the comparison is in the final gap. The
    $f^{*}$ reference on the real data is an independent deeper IRLS run
    ($\varepsilon_\text{thr}=10^{-14}$) cross-validated against CVXPY-Clarabel
    to ten significant digits (Section~\ref{sec:real_data}). Reproduced by
    \texttt{warm\_cold\_irls\_\{synthetic,real\}.csv} (IRLS) and
    \texttt{warm\_cold\_sgptl\_\{synthetic,real\}.csv} (SGPTL warm, and SGPTL
    cold on the synthetic); the SGPTL real-data cold entries are the $k=8000$
    samples of the long-run trace (Table~\ref{tab:sgptl-long-run-samples}).}
    \label{tab:warm-cold-summary}
\end{table}

\paragraph{Order-of-magnitude check on the SGPTL real-data gaps.}
The theorem reads
$\bar f^{k}-f^{*}\le L\norm{\mathbf{w}_{0}-\mathbf{w}^{*}}/(\gamma_{\min}\sqrt{k+1})$.
Dividing by $|f^{*}|$ and collapsing the numerator into the initial
relative gap $g_{0}^{\mathrm{rel}}=(f(\mathbf{w}_{0})-f^{*})/|f^{*}|$
gives the relative rule of thumb $\lesssim g_{0}^{\mathrm{rel}}/(\gamma_{\min}\sqrt{k})$.
With cold start, $\gamma_{\min}=0.05$, and $g_{0}^{\mathrm{rel}}\approx 130$
(synthetic), $3.3$ (\texttt{diabetes}), $3.5$ (\texttt{california}), at
$k=8\,000$ the envelope predicts $\sim\!30$, $\sim\!0.75$, $\sim\!0.78$
respectively. The observed relative gaps ($2.1\cdot 10^{-5}$,
$4.9\cdot 10^{-3}$, $1.8\cdot 10^{-2}$) sit one to six orders of magnitude
below it. The envelope is loose because the worst-case $L$ and the
initial distance $\norm{\mathbf{w}_{0}-\mathbf{w}^{*}}$ overestimate the
actual quantities on a well-conditioned design, and the deflection floor
binds only on degenerate steps
(Section~\ref{sec:dsm_convergence_conditions}). The margin is widest on
the synthetic; on \texttt{california} it is tightest, where the initial
distance is largest.

\paragraph{IRLS.}
Both starts converge to $f^{*}$ on every instance; the IRLS curves in
Figure~\ref{fig:irls-warm-cold} flatten to the
$\varepsilon_\text{stop}/\varepsilon_\text{thr}$ floor regardless of
start, so the comparison is in \emph{iteration count}, not final accuracy: cold needs
$\sim\!25\%$ more iterations than warm on \texttt{diabetes} ($1\,118$ vs
$894$) and $\sim\!13\%$ more on \texttt{california} ($115$ vs $102$);
on the synthetic the two coincide within rounding. The pattern matches
the linear rate (Section~\ref{sec:irls_convergence}), in which
$\norm{\mathbf{w}_{0}-\mathbf{w}^{*}}$ enters multiplicatively.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/irls_real_data_warm_vs_cold.pdf}
    \caption{IRLS warm (OLS) vs cold ($\mathbf{w}_{0}=\mathbf{0}$) start
    on the two ELM-transformed real datasets ($H=200$,
    $\lambda_\text{LASSO}=0.1$, $k_{\max}=2000$,
    $\varepsilon_\text{thr}=10^{-8}$, $\varepsilon_\text{stop}=10^{-8}$),
    log--log axes, relative gap $(f-f^{*})/|f^{*}|$ measured against an
    independent reference $f^{*}$ (a separate deeper IRLS run at
    $\varepsilon_\text{thr}=10^{-14}$, cross-validated against CVXPY-Clarabel,
    Section~\ref{sec:real_data}). Both starts descend to the
    $O(\varepsilon_\text{thr})$ smoothing floor of the displayed runs; the
    warm curve sits below the cold one throughout because
    $\norm{\mathbf{w}_{0}-\mathbf{w}^{*}}$ enters the linear-rate bound
    multiplicatively. The synthetic analogue is in
    Appendix~\ref{app:irls-synth-warm-cold}.}
    \label{fig:irls-warm-cold}
\end{figure}

\paragraph{SGPTL.}
The sublinear rate makes the warm--cold contrast much less pronounced
than for IRLS: the bound~\eqref{eq:def-rate-deflected} shrinks the
residual as $1/\sqrt{i}$, so unless $\mathbf{w}_{\mathrm{OLS}}$ is
already very close to $\mathbf{w}^{*}$ the two starts end the budget at
comparable gaps. On the synthetic instance they finish within a factor
of two (Figure~\ref{fig:warm-vs-cold}: cold $2.1\cdot 10^{-5}$ with $26$
$\delta$-contractions, warm $4.2\cdot 10^{-5}$ with $22$). On
\texttt{diabetes}-ELM the cold trajectory dips below the warm one past
$\sim\!10^{2}$ iterations and stays there ($4.9\cdot 10^{-3}$ vs
$1.5\cdot 10^{-2}$; contraction counts $17$ vs $12$, so the difference is
not driven by the patience schedule).

\texttt{California}-ELM is the qualitative outlier and the source of
the warm-start caveat referenced throughout this report.
$f(\mathbf{w}_{\mathrm{OLS}})$ sits within relative $1.95\cdot 10^{-4}$
of $f^{*}$ (absolute $0.46$) before SGPTL takes its first step, and the
Polyak stepsize
$\alpha_{i}=\beta_{i}(f(\mathbf{w}_{i})-\text{target})/\norm{\mathbf{d}_{i}}_{2}^{2}$
shrinks with the current gap, so the warm-started iterate barely moves
over $8\,000$ iterations; the warm-start number $1.95\cdot 10^{-4}$
measures how close OLS already is to $f^{*}$ on this instance, not the
residual SGPTL reduced from it. The cold start, starting at relative gap
$\sim\!2.5$ (absolute $\sim\!5\,900$), closes two decades down to
$1.8\cdot 10^{-2}$ over the same budget.
Figure~\ref{fig:real-data} (Section~\ref{sec:real_data}) overlays both
SGPTL starts.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/warm_vs_cold.pdf}
    \caption{SGPTL warm vs cold start on the synthetic instance
    ($H=100$, $M=300$, $\lambda_\text{LASSO}=0.1$, $i_{\max}=8000$,
    $\rho=0.7$, $\gamma_{\min}=0.05$, $R=1$). Left: record relative gap
    $(f-f^{*})/|f^{*}|$, log--log; both descend sublinearly. Right:
    $\delta_{i}$ staircase ($26$ contractions cold, $22$ warm).}
    \label{fig:warm-vs-cold}
\end{figure}

\paragraph{Defaults.}
For IRLS we use the OLS warm start everywhere in this report. The OLS
factorisation costs $O(MH^{2}+H^{3})$ in floating-point operations and,
measured directly, $0.13$\,ms on \texttt{diabetes}~($H=200$) and $2.9$\,ms
on \texttt{california}~($H=200$), against an IRLS body of $165$\,ms and
$1577$\,ms respectively (\texttt{warm\_start\_cost.csv}); the warm-start
share is below $0.2\%$ of the total in both regimes, so the
$\sim\!13$--$25\%$ iteration savings reported above more than offset
it. For SGPTL we adopt the cold start $\mathbf{w}_{0}=\mathbf{0}$ as the
default (it is the method's natural initialisation) and pass an explicit
warm start only where its final gap is empirically smaller, as on
\texttt{california} in Table~\ref{tab:real_data}.

\subsection{SGPTL beyond the submitted iteration budget}
\label{sec:dsm_long_run}
The estimate above predicts the SGPTL real-data gaps at $k=8\,000$ from
the rule of thumb $g_{0}^{\mathrm{rel}}/\sqrt{k}$. We extend the run to
characterise the asymptotic descent of the gap: SGPTL cold to
$i_{\max}=10^{7}$ on \texttt{diabetes}-ELM ($3.3$\,min wall-clock) and to
$i_{\max}=10^{6}$ on \texttt{california}-ELM ($23$\,min).
Figure~\ref{fig:sgptl-long-run} overlays the empirical record relative
gap against the heuristic envelope $g_{0}^{\mathrm{rel}}/\sqrt{i}$.

The empirical trace stays \emph{below} the envelope and descends faster
than its worst-case slope (Table~\ref{tab:sgptl-long-run-samples}). On
\texttt{diabetes} the relative gap drops from $4.9\cdot 10^{-3}$ at
$k=8\,000$ to $\le 10^{-12}$ by $k=8\cdot 10^{6}$: SGPTL reaches the
floating-point floor on this instance within the extended budget. On
\texttt{california} it descends from $1.8\cdot 10^{-2}$ at $k=8\,000$ to
$5.1\cdot 10^{-10}$ at $k=8\cdot 10^{5}$.

\begin{table}[H]
    \centering
    \small
    \setlength{\tabcolsep}{8pt}
    \begin{tabular}{rrrrr}
        \toprule
        & \multicolumn{2}{c}{\texttt{diabetes}-ELM ($g_{0}^{\mathrm{rel}}=2.34$)} & \multicolumn{2}{c}{\texttt{california}-ELM ($g_{0}^{\mathrm{rel}}=2.50$)} \\
        \cmidrule(lr){2-3}\cmidrule(lr){4-5}
        $k$ & observed & $g_{0}^{\mathrm{rel}}/\sqrt{k}$ & observed & $g_{0}^{\mathrm{rel}}/\sqrt{k}$ \\
        \midrule
        $8\,000$         & $4.92\cdot 10^{-3}$ & $2.62\cdot 10^{-2}$ & $1.76\cdot 10^{-2}$  & $2.80\cdot 10^{-2}$ \\
        $80\,000$        & $4.29\cdot 10^{-7}$ & $8.28\cdot 10^{-3}$ & $3.18\cdot 10^{-4}$  & $8.84\cdot 10^{-3}$ \\
        $800\,000$       & $6.42\cdot 10^{-9}$ & $2.62\cdot 10^{-3}$ & $5.10\cdot 10^{-10}$ & $2.80\cdot 10^{-3}$ \\
        $8\,000\,000$    & ${\le}10^{-12}$     & $8.28\cdot 10^{-4}$ & ---                  & --- \\
        $10\,000\,000$   & ${\le}10^{-12}$     & $7.41\cdot 10^{-4}$ & ---                  & --- \\
        \bottomrule
    \end{tabular}
    \caption{Observed record relative gap $(f-f^{*})/|f^{*}|$ of SGPTL
    versus the $g_{0}^{\mathrm{rel}}/\sqrt{k}$ envelope, cold start on the
    ELM-transformed real datasets.}
    \label{tab:sgptl-long-run-samples}
\end{table}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/sgptl_long_run.pdf}
    \caption{SGPTL at large iteration budgets, cold start on the
    ELM-transformed real datasets ($H=200$, $\lambda_\text{LASSO}=0.1$),
    log--log. Empirical record relative gap $(f-f^{*})/|f^{*}|$ (solid) vs
    the heuristic envelope $g_{0}^{\mathrm{rel}}/\sqrt{i}$ (dashed grey).
    Left: \texttt{diabetes} to $i_{\max}=10^{7}$, reaches the $10^{-12}$
    floor by $\sim\!8\cdot 10^{6}$ iterations. Right: \texttt{california}
    to $i_{\max}=8\cdot 10^{5}$, relative gap $\sim\!5\cdot 10^{-10}$ at
    the budget.}
    \label{fig:sgptl-long-run}
\end{figure}

% =====================================================================
\section{Hyperparameter tuning and technique choices}
\label{sec:params}

This section reports sensitivity sweeps on each algorithm separately:
the IRLS parameters $\varepsilon_\text{thr}$ and $\lambda_\text{LASSO}$
and the choice of inner solver (Cholesky vs Jacobi-preconditioned CG)
below, the SGPTL parameters $\gamma_{\min}$, $\delta_{0}$ and $\rho$
after.

\subsection{IRLS: \texorpdfstring{$\varepsilon_\text{thr}$}{eps\_thr}
and \texorpdfstring{$\lambda_\text{LASSO}$}{lambda\_LASSO}}

We sweep both at $H=100$, $M=400$, $100$ iterations
(Figure~\ref{fig:params-irls}). For
$\varepsilon_\text{thr}\in\{10^{-4},\dots,10^{-12}\}$ the final relative
gap tracks $\varepsilon_\text{thr}$ ($1.2\cdot 10^{-6}$, $1.3\cdot 10^{-8}$,
$4.6\cdot 10^{-10}$ at $10^{-6},10^{-8},10^{-10}$); $10^{-4}$ is too loose
(no sparsity) and $10^{-12}$ hits floating-point limits, so the
$10^{-8}$--$10^{-10}$ range is the default. Sweeping
$\lambda_\text{LASSO}\in\{0.01,\dots,1.0\}$ leaves the convergence speed
roughly invariant ($\lambda$ enters $\mathbf{Q}_{k}$ as a bounded
conditioning factor) while sparsity moves monotonically from $11\%$ to
$95\%$, as the optimality condition~\eqref{eq:optimality} implies.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/params_irls.pdf}
    \caption{IRLS sensitivity. Left: $\varepsilon_\text{thr}$ at
    $\lambda_\text{LASSO}=0.1$. Right: $\lambda_\text{LASSO}$ at
    $\varepsilon_\text{thr}=10^{-8}$.}
    \label{fig:params-irls}
\end{figure}

\subsection{IRLS: inner solver (Cholesky vs.\ CG)}
\label{sec:solver_comparison}
Chapter~\ref{chap:irls} leaves the inner solver of
$\mathbf{Q}_{k}\mathbf{w}_{k+1}=\mathbf{b}$ open between Cholesky
($O(H^{3}/3)$) and CG ($O(pH^{2})$ per step).
Table~\ref{tab:solver_comparison} compares them on $H=100$, $M=400$:
both reach the stopping criterion at iteration $265$ with identical
trajectories, but exact Cholesky takes $10.9$~ms against
$22.9$~ms for Jacobi-preconditioned CG. As IRLS drives inactive weights
to the cap $1/\varepsilon_\text{thr}=10^{8}$, $\kappa(\mathbf{Q}_{k})$
blows up; the Jacobi preconditioner on $\operatorname{diag}(\mathbf{Q}_{k})$ is what
keeps CG matching Cholesky iteration-by-iteration, but the iterative
overhead still doubles the wall-clock time. In the project regime
($M\gg H$, $H\le 1000$) Cholesky is the default: it is faster and the
exact solve preserves MM-descent unconditionally with no inner tolerance
to tune. CG becomes an alternative for $H$ large enough that the
$O(H^{3})$ term dominates.

\begin{table}[H]
    \centering
    \small
    \setlength{\tabcolsep}{8pt}
    \begin{tabular}{lrrrc}
        \toprule
        Solver & Total time (ms) & Iterations & Sparsity at $10^{-6}$ & Converged \\
        \midrule
        Cholesky & $10.9$ & $265$ & $86\%$ & Yes \\
        CG       & $22.9$ & $265$ & $86\%$ & Yes \\
        \bottomrule
    \end{tabular}
    \caption{IRLS solver comparison on $H=100$, $M=400$,
    $\lambda_\text{LASSO}=0.1$, $\varepsilon_\text{thr}=10^{-8}$,
    $k_{\max}=300$, OLS warm start. Both reach
    $\varepsilon_\text{stop}=10^{-10}$ at iteration $265$; sparsity at
    the final iterate under $|w_{i}|<10^{-6}$.}
    \label{tab:solver_comparison}
\end{table}

\subsection{SGPTL: the deflection floor \texorpdfstring{$\gamma_{\min}$}{gamma\_min}}
Theorem~\ref{thm:dsm_convergence} requires $\gamma_{i}\ge\gamma_{\min}>0$
to keep the stepsize clip $\beta_{i}=\min(\beta,\gamma_{i})$ uniformly
bounded away from zero
(Section~\ref{sec:dsm_convergence_conditions}); the greedy
formula~\eqref{eq:gamma_star} of
Section~\ref{sec:dsm_optimal_gamma} can otherwise drive $\gamma_{i}$
arbitrarily close to zero near stationarity. We run two sweeps below to
(i) check whether the floor is needed in practice on ELM~LASSO and
(ii) calibrate its value.

\paragraph{Does the floor matter? (binary test, $\gamma_{\min}\in\{0,\,0.05\}$).}
We compare the algorithm with no floor ($\gamma_{\min}=0$, the raw
greedy) against the floored variant ($\gamma_{\min}=0.05$) on a $3\times
5$ grid: three sizes $(H,M)$ equal to $(50,150)$, $(100,300)$ and
$(200,600)$, five seeds each. The same pattern emerges across the $30$
runs (Figure~\ref{fig:gamma-floor-test}; aggregate over the medium-size
block):

\begin{table}[H]
    \centering
    \small
    \setlength{\tabcolsep}{8pt}
    \begin{tabular}{ccccc}
        \toprule
        $\gamma_{\min}$ & final rel.\,gap & $\Pr\!\{\gamma_{i}\le 0.01\}$ & $\Pr\!\{\text{skip fires}\}$ & $\delta$-contr. \\
        \midrule
        $0.00$ & $4.4\cdot 10^{-2}$ & $99\%$ & $98\%$ & ${\sim}\,2$ \\
        $0.05$ & $1.1\cdot 10^{-4}$ & $0\%$  & $0\%$  & ${\sim}\,19$ \\
        \bottomrule
    \end{tabular}
\end{table}

The mechanism behind the failure mode of $\gamma_{\min}=0$ is a
chain reaction visible on the figure's leftmost panel. Near stationarity
the greedy $\gamma^{*}$ from~\eqref{eq:gamma_star} approaches zero
because consecutive subgradients align and the numerator
$\norm{\mathbf{d}_{i-1}}^{2}-\inner{\mathbf{g}_{i}}{\mathbf{d}_{i-1}}$
vanishes. With no floor, $\gamma_{i}$ collapses below $10^{-10}$ from
roughly iteration~$50$ onward and stays there for the rest of the run.
The clip $\beta_{i}=\min(1,\gamma_{i})$ collapses with it, the Polyak
step~\eqref{eq:sr_step} becomes positive but minuscule, and once $f_{i}$
no longer beats the target the numerator-skip safeguard fires on $98\%$
of iterations: the
iterate freezes and $\delta$ contracts only $\sim\!2$ times instead of
$\sim\!19$, leaving the relative gap stuck at $\sim\!4.4\cdot 10^{-2}$
(centre and right panels). With $\gamma_{\min}=0.05$ the floor binds whenever the
greedy would hit zero, $\beta_{i}$ stays $\ge 0.05$, the Polyak step
remains effective, and the staircase descent of $\delta$ that the theory
predicts takes place; the gap drops by two more orders of magnitude.
This failure depends on the algorithm's greedy $\gamma$ choice rather
than on the specific instance, and we observed it on all $15$ unfloored
seeds.

\paragraph{Which value of $\gamma_{\min}$ (calibration sweep).}
We sweep $\gamma_{\min}$ over $0.05$, $0.1$, $0.2$ and $0.5$ on the
convergence instance. The final relative gap lies in $[10^{-7},10^{-5}]$
across the four values, with $\gamma_{\min}=0.05$ marginally best. Larger
values force the clip to bind on iterations where the greedy
$\gamma_{i}$ would otherwise lie strictly inside $(\gamma_{\min},1]$,
so we adopt $\gamma_{\min}=0.05$.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/gamma_floor_test.pdf}
    \caption{Floor test on ELM~LASSO ($H=100$, $M=300$,
    $\lambda_\text{LASSO}=0.1$, $i_{\max}=4000$, $\rho=0.7$), single seed
    shown. Left: trajectory of $\gamma_{i}$ over the run (log--log).
    Without the floor, $\gamma_{i}$ collapses below $10^{-10}$ around
    iteration~$50$ and stays there; with $\gamma_{\min}=0.05$ it sits on
    the floor (dotted grey line) for the rest of the run. Centre:
    target gap $\delta_{i}$; the unfloored $\delta$ contracts twice and
    stops, the floored one descends in the staircase the theory expects.
    Right: record relative gap $(\bar f^{i}-f^{*})/|f^{*}|$; the floor buys
    two orders of magnitude. The aggregate over the $30$ runs of the binary grid agrees
    with this single-seed snapshot.}
    \label{fig:gamma-floor-test}
\end{figure}

\subsection{SGPTL: \texorpdfstring{$\delta_{0}$}{delta\_0} and
\texorpdfstring{$\rho$}{rho}}
Of the four target-level parameters of SGPTL, $\beta$ and $R$ are
settled analytically in Section~\ref{sec:dsm_parameters} and not swept
here. The remaining two, $\delta_{0}=c\,f(\mathbf{w}_{0})$ and $\rho$,
have empirical tradeoffs and are calibrated below. The sweeps of
Figure~\ref{fig:params-dsm} ($H=100$, $M=400$, $5000$ iterations) show
the method is robust to both: $c\in\{0.01,\dots,1.0\}$ at $\rho=0.7$
gives final relative gaps in $[4.2\cdot 10^{-5},1.2\cdot 10^{-4}]$, within
one order of magnitude across two decades of $\delta_{0}$;
$\rho\in\{0.3,\dots,0.95\}$ at $c=0.1$ gives
$1.7\cdot 10^{-5}$ up to $6.1\cdot 10^{-4}$ with $7$ to $91$
contractions. Small $\rho$ wins on this instance, but a smoke test over
the synthetic and the two ELM matrices places the per-instance best at
$\rho=0.3,0.3,0.7$; only $\rho\in[0.3,0.7]$ is within a factor of two of
the best everywhere, and $\rho\ge 0.9$ loses by one to two orders of
magnitude. We adopt $\rho=0.7$ and $c=0.1$ as robust defaults.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/params_dsm.pdf}
    \caption{SGPTL sensitivity ($H=100$, $M=400$, OLS warm start, $5000$
    iterations). Left: $\delta_{0}$ as a fraction of $f(\mathbf{w}_{0})$
    at $\rho=0.7$. Right: $\rho$ at $\delta_{0}=0.1\,f(\mathbf{w}_{0})$;
    relative gaps and contraction counts grow monotonically with $\rho$.}
    \label{fig:params-dsm}
\end{figure}

\subsection{SGPTL: \texorpdfstring{$\delta_{0}$}{delta\_0} against the
\texorpdfstring{$f^{*}$}{f*}-based scale (abstract sensitivity check)}
\label{sec:delta0_oracle}
The operational choice is $\delta_{0}=c\,f(\mathbf{w}_{0})$, settled in
Section~\ref{sec:dsm_parameters} and used in every run of this chapter.
The comparison below is purely an abstract sensitivity study: we ask, as
a sanity check, how the admissible scale would compare against the
hypothetical $\delta_{0}=c\,f^{*}$ \emph{if} $f^{*}$ were known. Tuning
algorithmic parameters against $f^{*}$ is not allowed in practice (it
would amount to using the optimum to set the optimizer, the
optimisation analogue of training on the test set), so the
$f^{*}$-based scale enters only as a reference point in this and the
next subsection and is never used to choose the default.

We run both scales on five instances over $c\in\{0.01,\dots,1\}$. The
admissible scale matches the oracle in the median on every instance
(ratio $\approx 1$), and the per-run ratio stays within a factor of two
except on the synthetic, where the five-seed aggregate of two
individually-noisy $\sim\!10^{-5}$ final gaps widens the spread
(Table~\ref{tab:delta0-ratio}).
The one exception is \texttt{diabetes}~$H=50$ at $c\ge 0.5$, where an
over-aggressive $\delta_{0}$ overshoots the sufficient-descent test
from the first iteration and both scales stall at a common plateau (a
regime limit of the sweep, not a finding about the scales). The full
per-instance grid is in Appendix~\ref{app:delta0-families}.

\begin{table}[H]
    \centering
    \small
    \setlength{\tabcolsep}{8pt}
    \begin{tabular}{lc}
        \toprule
        instance & admissible / oracle gap ratio (median) \\
        \midrule
        synthetic ($5$ seeds, $c\le 0.1$)   & $[0.18,\,2.07]$ ($0.90$) \\
        \texttt{diabetes}~$H=50$ ($c\le 0.1$) & $[0.73,\,1.96]$ ($1.16$) \\
        \texttt{diabetes}~$H=200$              & $[0.87,\,1.04]$ ($1.00$) \\
        \texttt{california}~$H=50$ (cold)      & $[0.72,\,1.41]$ ($0.99$) \\
        \texttt{california}~$H=200$ (cold)     & $[0.96,\,1.07]$ ($1.00$) \\
        \bottomrule
    \end{tabular}
    \caption{Ratio of the admissible scale $\delta_{0}=c\,f(\mathbf{w}_{0})$
    to the inadmissible oracle $\delta_{0}=c\,f^{*}$ on the final gap, in the
    working regime (range over runs, median in parentheses). The median ratio
    is $\approx 1$ on every instance; the per-run spread stays within a factor
    of two except on the synthetic, where aggregating five seeds and ratioing
    two individually-noisy $\sim\!10^{-5}$ gaps widens it. Full grid:
    Appendix~\ref{app:delta0-families}.}
    \label{tab:delta0-ratio}
\end{table}

% =====================================================================
\section{IRLS versus SGPTL}
\label{sec:comparison_results}
Chapter~\ref{chap:comparison} contrasts the linear cubic-per-iteration
IRLS with the sublinear cheap-per-iteration SGPTL and predicts IRLS
dominance for moderate-to-high accuracy in the $M\gg H$ regime. The
following subsections test this prediction on solution quality and
sparsity, scaling, and iterations to a target.

\subsection{Solution quality and sparsity}
\label{sec:quality}
On $H=50$, $M=200$, $\lambda_\text{LASSO}=0.1$ (reference sparsity
$88\%$), IRLS reaches relative gap $10^{-6}$ in $125$ iterations at
$\|\mathbf{w}_\text{IRLS}-\mathbf{w}^{*}\|_{2}=2.95\cdot 10^{-6}$; SGPTL
is two orders of magnitude looser, at relative gap $\sim\!2\cdot 10^{-4}$.
The two need
\emph{different} sparsity thresholds, as Section~\ref{sec:dsm_properties}
predicts: IRLS pins inactive weights near $\varepsilon_\text{thr}=10^{-8}$,
whereas SGPTL's inactive components sit at the residual-rate scale
$L/(\gamma_{\min}\sqrt{i})\in[10^{-4},10^{-3}]$. Applying the
\emph{same} hard threshold to both (Table~\ref{tab:support_recovery}):
at $10^{-6}$ SGPTL's $F_{1}$ collapses to $0.36$ because its inactive
coefficients sit above the cutoff; at $10^{-3}$, above the residual
floor, both recover the support exactly.

\begin{table}[H]
    \centering
    \begin{tabular}{rrrrrr}
        \toprule
        threshold & method & sparsity & precision & recall & F1 \\
        \midrule
        $10^{-6}$ & IRLS  & $86\%$ & $0.857$ & $1.000$ & $0.923$ \\
        $10^{-6}$ & SGPTL & $46\%$ & $0.222$ & $1.000$ & $0.364$ \\
        $10^{-6}$ & ground truth  & $88\%$ & $1.000$ & $1.000$ & $1.000$ \\
        \midrule
        $10^{-3}$ & IRLS  & $88\%$ & $1.000$ & $1.000$ & $1.000$ \\
        $10^{-3}$ & SGPTL & $88\%$ & $1.000$ & $1.000$ & $1.000$ \\
        $10^{-3}$ & ground truth  & $88\%$ & $1.000$ & $1.000$ & $1.000$ \\
        \bottomrule
    \end{tabular}
    \caption{Support recovery on $H=50$, $M=200$,
    $\lambda_\text{LASSO}=0.1$ (reference sparsity $88\%$): same hard
    threshold on both methods, precision/recall/F1 against the support of
    $\mathbf{w}^{*}$. At $10^{-3}$ both recover it exactly; at $10^{-6}$
    SGPTL fails because its inactive coefficients sit at the
    $L/\sqrt{i}\sim 10^{-3}$ scale.}
    \label{tab:support_recovery}
\end{table}

\subsection{Scalability}
\label{sec:scalability}

\paragraph{Two regimes.}
We measure cost growth in two regimes. \emph{Synthetic} ($M=5H$): $H$ and
$M$ grow together, over five sizes 50, 100, 500, 1000, 2000, so the
per-iteration costs of Table~\ref{tab:comp_cost} read $O(H^{3})$ for IRLS and
$O(H^{2})$ for SGPTL. \emph{Real} (\texttt{california}, $M=16512$ fixed): only
the ELM hidden size $H$ grows, over 50, 100, 200, 500, 1000. Both use a fixed iteration
budget, $100$ Cholesky iterations for IRLS and $3000$ subgradient iterations
for SGPTL, except that on \texttt{california} IRLS met its stopping tolerance
before the cap at $H\le 100$ ($7$ and $37$ iterations) and reached the cap at
$H\ge 200$. Each time is the median of five runs after one warm-up, with an
idle gap between problems, on one CPU with default multi-threaded BLAS and a
single problem seed.

\paragraph{FLOP count and wall-clock.}
For each algorithm we report two quantities on the same $H$ axis. The
\emph{analytic FLOP count} sums the operations the implementation runs: the
one-time $\mathbf X^\top\mathbf X$, and per iteration a Cholesky factorisation
($H^{3}/3$ flops~\cite[Lecture 23]{trefethen1997numerical}) plus one residual
$\mathbf X\mathbf w$ for IRLS, two matrix--vector products ($2MH$ each) for
SGPTL, evaluated at the measured iteration counts. The \emph{wall-clock} is the
realised cost. The FLOP count is implementation-independent and exact; the
fitted wall-clock exponent shifts by $\pm 0.2$--$0.3$ between runs with the BLAS
efficiency and thermal state of the machine, so we read the scaling from the
FLOP count and use wall-clock for the absolute head-to-head and for the
throughput it implies. Fitted log--log slopes (upper range) are collected in
Table~\ref{tab:scalability}; timings are in Table~\ref{tab:scalability-time}.

\paragraph{Synthetic regime.}
The FLOP count scales as $H^{2.97}$ for IRLS and $H^{2.00}$ for SGPTL, which is
the $O(H^{3})$ and $O(H^{2})$ per-iteration cost of Table~\ref{tab:comp_cost}
instantiated at $M=5H$, with no lower-order contamination in the tested range.
IRLS wall-clock ($H^{2.95}$) follows its FLOP count; SGPTL wall-clock
($H^{2.84}$) grows faster than its quadratic FLOP count. The throughput panel
of Figure~\ref{fig:scalability} (FLOP divided by wall-clock) shows why:
IRLS reaches and holds $\sim\!80$--$90$\,GFLOP/s once the matrices are large
enough for BLAS to run near peak, while SGPTL throughput drops at $H\approx1000$,
consistent with the working set ($\mathbf X$ is $5000\times1000$, $\sim\!40$\,MB)
exceeding cache and leaving the matrix--vector loop memory-bandwidth bound. IRLS is faster in
wall-clock at every $H$ ($3.3$\,s vs $34$\,s at $H=2000$): the quadratic FLOP
count of SGPTL would overtake the cubic one of IRLS only past the largest size
we test, and the implementation effects push that crossover further out still.

\paragraph{Real regime (fixed $M$).}
With $M$ fixed and large the exponents drop. IRLS scales as $H^{2.17}$ in FLOPs:
the one-time $O(MH^{2})$ dominates at small $H$ and the per-iteration $O(H^{3})$
term takes over as $H$ grows. SGPTL scales as $H^{1.00}$, since $O(MH)$ is linear
in $H$ at fixed $M$. Wall-clock tracks both ($H^{2.02}$ and $H^{1.12}$), and IRLS
is again faster throughout ($1.3$\,s vs $40$\,s at $H=1000$). The synthetic
sweep and this one probe different regimes: here $M$ stays fixed, so the
$MH^{2}$ and $MH$ terms stay bounded and the exponent gap is narrower than at
$M=5H$.

\paragraph{Fixed budget vs cost to a target.}
Both tables fix iterations, not accuracy. To reach a target $\varepsilon$
Theorem~\ref{thm:dsm_convergence} forces
$\Omega(L^{2}/(\gamma_{\min}^{2}\varepsilon^{2}))$ SGPTL iterations with $L$
growing in $H$, so the cost-to-accuracy ratio is wider than a fixed-budget time
suggests, as quantified in Section~\ref{sec:iters-to-eps}.

\begin{table}[H]
    \centering
    \begin{tabular}{llcc}
        \toprule
        Regime & Algorithm & wall-clock & FLOP count \\
        \midrule
        synthetic, $M=5H$       & IRLS  & $H^{2.95}$ & $H^{2.97}$ \\
                                & SGPTL & $H^{2.84}$ & $H^{2.00}$ \\
        \midrule
        \texttt{california}, $M=16512$ & IRLS  & $H^{2.02}$ & $H^{2.17}$ \\
                                & SGPTL & $H^{1.12}$ & $H^{1.00}$ \\
        \bottomrule
    \end{tabular}
    \caption{Fitted log--log scaling exponents in $H$ (slope over the upper
    half of the range). The FLOP count matches the per-iteration complexity of
    Table~\ref{tab:comp_cost}; wall-clock matches it except for SGPTL on the
    synthetic sweep, where the memory-bandwidth cliff at $H\approx1000$ steepens
    the measured slope (Figure~\ref{fig:scalability}, throughput panel).}
    \label{tab:scalability}
\end{table}

\begin{table}[H]
    \centering
    \begin{tabular}{rrrrr}
        \toprule
        $H$ & $M$ & $k_\text{IRLS}$ & $t_\text{IRLS}$ (s) & $t_\text{SGPTL}$ (s) \\
        \midrule
        \multicolumn{5}{l}{\emph{synthetic, $M=5H$}}\\
        $  50$ & $  250$ & $100$ & $0.003$ & $\phantom{0}0.085$ \\
        $ 100$ & $  500$ & $100$ & $0.006$ & $\phantom{0}0.066$ \\
        $ 500$ & $ 2500$ & $100$ & $0.055$ & $\phantom{0}0.672$ \\
        $1000$ & $ 5000$ & $100$ & $0.546$ & $\phantom{0}7.937$ \\
        $2000$ & $10000$ & $100$ & $3.271$ & $34.281$ \\
        \midrule
        \multicolumn{5}{l}{\emph{\texttt{california}, $M=16512$ fixed}}\\
        $  50$ & $16512$ & $\phantom{0}7$ & $0.001$ & $\phantom{0}0.942$ \\
        $ 100$ & $16512$ & $37$ & $0.006$ & $\phantom{0}2.059$ \\
        $ 200$ & $16512$ & $100$ & $0.049$ & $\phantom{0}6.498$ \\
        $ 500$ & $16512$ & $100$ & $0.314$ & $18.287$ \\
        $1000$ & $16512$ & $100$ & $1.269$ & $39.512$ \\
        \bottomrule
    \end{tabular}
    \caption{Median wall-clock at the fixed iteration budget ($k_\text{IRLS}\le
    100$, $3000$ SGPTL iterations). $k_\text{IRLS}$ is the iteration count
    actually run; on \texttt{california} IRLS converges before the cap at
    $H\le 100$.}
    \label{tab:scalability-time}
\end{table}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/scalability.pdf}
    \caption{Synthetic regime ($M=5H$). Left: median wall-clock (solid) and
    analytic FLOP count (dashed, anchored on the largest-$H$ point), log--log,
    with fitted slopes annotated. Right: effective throughput, FLOP count
    divided by wall-clock. IRLS rises toward the BLAS peak; SGPTL throughput
    drops near $H=1000$, where the working set exceeds cache.}
    \label{fig:scalability}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/scalability_real.pdf}
    \caption{Real regime (\texttt{california}, $M=16512$ fixed): neuron scaling
    in $H$. Left: wall-clock vs FLOP count; right: throughput. With $M$ fixed
    the FLOP exponents fall to $\approx\!2$ (IRLS) and $\approx\!1$ (SGPTL), and
    wall-clock tracks them.}
    \label{fig:scalability-real}
\end{figure}

\subsection{Iterations to a target accuracy}
\label{sec:iters-to-eps}
Table~\ref{tab:iters-to-eps} and Figure~\ref{fig:comparison} report the
cost to reach relative gap $(f-f^{*})/|f^{*}|\le\varepsilon$ on the
moderate problem ($H=50$, $M=200$). At the loosest accuracy the two are
competitive ($5$ vs $1$ at $10^{-1}$); past that the gap widens fast:
SGPTL needs $203,1008,4193$ iterations for $10^{-2},10^{-3},10^{-4}$
against IRLS's $4,10,25$ ($74$\,ms vs $0.7$\,ms at $10^{-4}$). The
per-decade ratio predicted by Chapter~\ref{chap:comparison} (constant
increment for IRLS, $\sim\!100\times$ multiplier for SGPTL) is reproduced
here; SGPTL does not reach $10^{-6}$ within $30\,000$ iterations, IRLS
reaches it in $125$.

\begin{table}[H]
    \centering
    \begin{tabular}{rrrrr}
        \toprule
        $\varepsilon$ & $k_\text{IRLS}$ & $t_\text{IRLS}$ (s) &
        $i_\text{SGPTL}$ & $t_\text{SGPTL}$ (s) \\
        \midrule
        $10^{-1}$ & $  1$ & $5.7\cdot 10^{-5}$ & $    5$ & $1.3\cdot 10^{-4}$ \\
        $10^{-2}$ & $  4$ & $1.6\cdot 10^{-4}$ & $  203$ & $3.7\cdot 10^{-3}$ \\
        $10^{-3}$ & $ 10$ & $3.3\cdot 10^{-4}$ & $ 1008$ & $1.8\cdot 10^{-2}$ \\
        $10^{-4}$ & $ 25$ & $7.3\cdot 10^{-4}$ & $ 4193$ & $7.4\cdot 10^{-2}$ \\
        $10^{-6}$ & $125$ & $3.4\cdot 10^{-3}$ & --- & --- \\
        \bottomrule
    \end{tabular}
    \caption{Iterations and time to reach relative gap
    $(f-f^{*})/|f^{*}|\le\varepsilon$ on
    $H=50$, $M=200$, $\lambda_\text{LASSO}=0.1$.}
    \label{tab:iters-to-eps}
\end{table}

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/comparison_irls_dsm.pdf}
    \caption{IRLS vs SGPTL on $H=50$, $M=200$, $\lambda_\text{LASSO}=0.1$,
    log--log, relative gap $(f-f^{*})/|f^{*}|$. Left: vs iteration. Right:
    vs CPU time. SGPTL falls below $10^{-2}$ in $203$ iterations and
    $10^{-3}$ in $1008$ then enters the sublinear regime; IRLS reaches
    $10^{-6}$ in $125$ iterations ($\sim\!3$~ms).}
    \label{fig:comparison}
\end{figure}

% =====================================================================
\section{Validation on real datasets}
\label{sec:real_data}
We need an independent $f^{*}$ to report gaps on these two instances. The
natural off-the-shelf candidate is sklearn's coordinate-descent
\texttt{Lasso}, but on the ELM-transformed matrices sklearn never reaches
its own \texttt{tol} stop: at $\texttt{max\_iter}=10^{5}$,
$\texttt{tol}=10^{-10}$, it returns at the iteration cap with a final
duality gap two to six orders of magnitude above tolerance, and a
residual relative gap to $f^{*}$ of $+2.6\cdot 10^{-5}$ on
\texttt{diabetes} and $+1.2\cdot 10^{-3}$ on \texttt{california}.
sklearn CD is therefore not a usable oracle on these problems, and we
build $f^{*}$ instead by running IRLS to convergence
($k_{\max}=3000,\varepsilon_\text{thr}=10^{-14}$) and cross-validating it
against CVXPY's interior-point solver Clarabel. The two are structurally
different (MM~+~linear solve vs.\ primal--dual interior point) and agree
to ten significant digits on both instances.

Both datasets use $H=200$ sigmoid units, $\lambda_\text{LASSO}=0.1$.
The comparison criterion is fixed-accuracy rather than fixed-iteration:
each method runs until the relative gap $(f - f^{*})/|f^{*}| \le 10^{-6}$,
and the cost is reported in iterations and wall time
(Table~\ref{tab:real_data}). IRLS runs with tight smoothing
$\varepsilon_\text{thr}=10^{-12}$ from the OLS warm start; CVXPY-Clarabel
runs at \texttt{tol\_gap}$=10^{-9}$; SGPTL runs cold from
$\mathbf{w}_{0}=\mathbf{0}$ with $\beta=1$, $\gamma_{\min}=0.05$, $R=1$,
$\rho=0.7$, $\delta_{0}=0.1\,f(\mathbf{w}_{0})$. The SGPTL iter-to-target
counts and wall-times in Table~\ref{tab:real_data} are measured directly:
we rerun SGPTL with $i_{\max}$ set just above the relative-target crossing
($9\cdot 10^{4}$ on \texttt{diabetes}, $3.6\cdot 10^{5}$ on
\texttt{california}) and read the wall-clock at the first iteration where
the record relative gap falls below $10^{-6}$
(\texttt{real\_data\_crossing.csv}). The full long-run trajectory to
$i_{\max}=10^{7}$/$10^{6}$ is the one of Section~\ref{sec:dsm_long_run}.

\begin{table}[H]
    \centering
    \small
    \setlength{\tabcolsep}{5pt}
    \begin{tabular}{lrrrr}
        \toprule
        method & iter to rel.\,gap$\le 10^{-6}$ & wall time & sp.\,$10^{-3}$ & test MSE \\
        \midrule
        \multicolumn{5}{l}{\emph{Diabetes} ($M=354$, $f^{*}=52.948763$)} \\
        \cmidrule(lr){1-5}
        CVXPY-Clarabel & $10$              & $92$\,ms            & $19\%$ & $0.898$ \\
        IRLS           & $56$              & $4.3$\,ms           & $19\%$ & $0.898$ \\
        SGPTL (cold)   & $5.63\cdot 10^{4}$ & $1.1$\,s           & $19\%$ & $0.898$ \\
        Ridge          & $1$ (closed form) & $< 1$\,ms           & ---    & $0.942$ \\
        \midrule
        \multicolumn{5}{l}{\emph{California} ($M=16512$, $f^{*}=2358.019449$)} \\
        \cmidrule(lr){1-5}
        CVXPY-Clarabel & $8$               & $6.3$\,s            & $\phantom{0}4\%$ & $0.307$ \\
        IRLS           & $3$               & $1.3$\,ms           & $\phantom{0}4\%$ & $0.307$ \\
        SGPTL (cold)   & $2.97\cdot 10^{5}$ & $567$\,s ($9.4$\,min) & $\phantom{0}4\%$ & $0.307$ \\
        Ridge          & $1$ (closed form) & $< 5$\,ms           & ---              & $0.307$ \\
        \bottomrule
    \end{tabular}
    \caption{Cost to reach a uniform relative target accuracy
    $(f - f^{*})/|f^{*}| \le 10^{-6}$ on the two ELM-transformed real datasets,
    $H=200$, $\lambda_\text{LASSO}=0.1$, single-thread CPU timings. The
    reference $f^{*}$ is the cross-validated value of the introduction
    (IRLS at $\varepsilon_\text{thr}=10^{-14}$, $k_{\max}=3000$ against
    CVXPY-Clarabel at \texttt{tol\_gap}$=10^{-9}$). The
    \emph{iter} column counts the inner loop of each method (Newton
    interior-point steps for Clarabel, IRLS outer iterations each one
    Cholesky solve, SGPTL subgradient steps); Ridge requires one
    Cholesky solve and is given as a sparsity-less baseline. Wall times
    measure the iteration loop only; the one-time OLS warm-start
    factorisation (\,$0.13$\,ms on \texttt{diabetes}, $2.9$\,ms on
    \texttt{california}, Section~\ref{sec:warm_cold}) is excluded, as
    is the analogous one-time $\delta_{0}=c\,f(\mathbf{w}_{0})$
    evaluation paid by SGPTL ($O(M)$, microseconds). Sparsity counts
    $|w_{i}|<10^{-3}$ (Section~\ref{sec:quality}); test MSE is on the
    held-out $20\%$ split and is reported for completeness only
    (cf.\ ``reading the table'' paragraph).}
    \label{tab:real_data}
\end{table}

\paragraph{Reading the table.}
At the same target accuracy the three optimisers agree on the solution
(same sparsity, all distinct from Ridge on \texttt{diabetes}) but
disagree on cost by orders of magnitude. The test-MSE columns are
included for completeness and are not part of the optimisation
comparison: out-of-sample error is a learning metric, while the
question here is how cheaply each algorithm reaches the same training
optimum.
IRLS is fastest in wall time on both datasets ($4.3$\,ms,
$1.3$\,ms): the rate is linear in the active-set regime, and the
linear cost per iteration ($O(MH^{2}+H^{3})$) is paid only a few dozen
times. Clarabel needs the fewest iterations ($8$--$10$) but each one
factorises an $H\!\times\!H$ system, so on \texttt{california} ($M=16512$)
its wall time is $\approx\!5000\times$ that of IRLS; the iteration
ranking and the wall-time ranking diverge whenever the per-iteration
cost differs sharply across methods. SGPTL pays the sublinear rate:
$5.6\cdot 10^{4}$ iterations on \texttt{diabetes} and $3.0\cdot 10^{5}$
on \texttt{california} to reach the same relative $10^{-6}$ target,
$\sim\!10^{3}$ to $10^{5}$ times the iteration count of the other two and
a wall-time loss of $\sim\!10^{2}$--$10^{5}\times$. Figure~\ref{fig:real-data}
gives the per-iteration view: IRLS hits its accuracy floor at
$\sim\!100$ iterations, while SGPTL reaches the same $f^{*}$ floor only
after $10^{5}$--$10^{7}$ iterations at the sublinear
$g_{0}^{\mathrm{rel}}/\sqrt{i}$ rate of Theorem~\ref{thm:dsm_convergence}.

\begin{figure}[H]
    \centering
    \includegraphics[width=\textwidth]{images/real_data_convergence.pdf}
    \caption{Convergence on the two real datasets, log--log,
    relative gap $(f - f^{*})/|f^{*}|$ against iteration. IRLS (blue, $k_{\max}=2000$,
    $\varepsilon_\text{thr}=10^{-12}$) reaches the $f^{*}$ floor in
    $\sim\!100$ iterations on both. SGPTL cold (red, long-run trace
    from Section~\ref{sec:dsm_long_run}: $i_{\max}=10^{7}$ on
    \texttt{diabetes}, $10^{6}$ on \texttt{california}) descends at the
    sublinear $g_{0}^{\mathrm{rel}}/\sqrt{i}$ rate of
    Theorem~\ref{thm:dsm_convergence} to relative gap
    $\le 10^{-12}$ on \texttt{diabetes} and $\approx 4\cdot 10^{-10}$ on
    \texttt{california}. sklearn coordinate descent (green dotted) is the
    third-party convergence trace: the LASSO objective after a capped
    number of coordinate sweeps, run cold to $\texttt{max\_iter}=10^{5}$ at
    $\texttt{tol}=10^{-16}$ so it never stops early. It returns at the
    iteration cap still above $f^{*}$, without reaching the $10^{-6}$
    target on either dataset.}
    \label{fig:real-data}
\end{figure}

% =====================================================================
\section{Initial design and corrections}
\label{sec:initial_design}
Two SGPTL design choices in Algorithm~\ref{alg:dsm} were reached only
after correcting an earlier implementation. We document each as
\emph{before}~/~\emph{why~wrong}~/~\emph{after}~/~\emph{why~right}, then
compare the numbers the two implementations produce on the convergence
instance.

\paragraph{Target-level scale $\delta_{0}$.}
\emph{Before:} $\delta_{0}=c\,f^{*}$.
\emph{Why wrong:} $f^{*}$ is the very quantity SGPTL exists to estimate,
so using it to set an algorithmic parameter assumes access to the answer
and disqualifies the result.
\emph{After:} $\delta_{0}=c\,f(\mathbf{w}_{0})$.
\emph{Why right:} $f(\mathbf{w}_{0})$ is computable from the starting
point alone; the sweep in Section~\ref{sec:delta0_oracle} confirms it
tracks the oracle scale within a factor of two on every test instance,
so the admissible choice loses nothing in practice.

\paragraph{Contraction trigger and the radius $R$.}
\emph{Before:} $R=10\sqrt{i_{\max}}\!\approx\!894$ together with an
iteration-count fallback that contracted $\delta$ every
$\lceil i_{\max}/30\rceil$ steps.
\emph{Why wrong:} the realistic per-iteration step length on this
problem is $\alpha_{i}\norm{\mathbf{d}_{i}}\sim 10^{-3}$, so cumulative
travel never reaches $R\!\approx\!894$ and the travel-distance branch
never fires; \emph{every} contraction is driven by the iteration
counter, which is inconsistent
with~\cite[Lemma~3.8]{dantonio2009deflected}'s requirement that each
contraction follow $r_{k}>R$ travel.
\emph{After:} $R=1$ matched to the realistic step length, fallback
removed.
\emph{Why right:} contractions now fire on travel as the lemma
requires, so the $\delta$ schedule reflects the actual descent.

\paragraph{Before vs.\ after on the convergence instance.}
Table~\ref{tab:before-after-numbers} compares the two implementations
on the same instance ($H=100$, $M=300$, $\lambda_\text{LASSO}=0.1$,
$i_{\max}=8000$). The pre-correction contraction count is $\sim\!95$
(all from the fallback, none from travel-distance); post-correction it
is $\sim\!22$ (all from travel-distance). The record gap drops by three
orders of magnitude under the old setup, but this is not convergence:
it is the $\delta$ schedule collapsing on a counter rather than on the
optimisation state, decoupling target levels from actual progress. The
post-correction record relative gap $4.2\cdot 10^{-5}$ is the figure the
theory of Theorem~\ref{thm:dsm_convergence} bounds for SGPTL on this instance.
The qualitative ranking against IRLS (Chapter~\ref{chap:comparison}) is
unaffected.

\begin{table}[H]
    \centering
    \small
    \setlength{\tabcolsep}{6pt}
    \begin{tabular}{lrr}
        \toprule
         & before (wrong) & after (correct) \\
        \midrule
        $\delta_{0}$                     & $c\,f^{*}$ (oracle) & $c\,f(\mathbf{w}_{0})$ \\
        $R$ used                         & $10\sqrt{i_{\max}}\approx 894$ & $1$ \\
        iteration-count fallback         & yes & no \\
        contractions from $r_{k}>R$       & $0$ & $\sim\!22$ \\
        contractions from fallback       & $\sim\!95$ & $0$ \\
        final record rel.\,gap $(\bar f - f^{*})/|f^{*}|$ & $\sim\!5.8\cdot 10^{-8}$ & $4.2\cdot 10^{-5}$ \\
        \bottomrule
    \end{tabular}
    \caption{Pre- and post-correction SGPTL numbers on the convergence
    instance ($H=100$, $M=300$, $\lambda_\text{LASSO}=0.1$,
    $i_{\max}=8000$). The pre-correction record gap is lower in absolute
    terms but is produced by a $\delta$ schedule that does not track
    descent, so its meaning as a measure of optimisation progress is
    not what the theorem bounds. The \emph{before} column documents the
    removed pre-correction prototype and is not regenerable from the current
    code; the \emph{after} column matches \texttt{convergence\_instance.csv}.}
    \label{tab:before-after-numbers}
\end{table}
